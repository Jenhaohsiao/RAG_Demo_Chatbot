# Environment Variables Template for RAG Demo Chatbot
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env (for default config)
# 2. Create .env.local for sensitive data (API keys)
# 3. Both files will be loaded automatically
#
# =============================================================================
# Gemini API Key (REQUIRED - Put in .env.local)
# =============================================================================
# Create .env.local with:
# GEMINI_API_KEY=your_actual_api_key_here
#
# Get your free API key at: https://aistudio.google.com/app/apikey
# =============================================================================
#
# Note: .env.local is in .gitignore and will NOT be committed to Git

# Qdrant Configuration
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_MODE=docker  # Options: embedded, docker, cloud

# Session Configuration
SESSION_TTL_MINUTES=30

# RAG Configuration
SIMILARITY_THRESHOLD=0.6  # Reduced from 0.7 to improve recall while maintaining precision
CHUNK_SIZE=512
CHUNK_OVERLAP=128# Gemini Configuration
GEMINI_MODEL=gemini-1.5-flash  # Cheapest model suitable for RAG
GEMINI_EMBEDDING_MODEL=text-embedding-004
GEMINI_TEMPERATURE=0.1

# For Qdrant Cloud (if QDRANT_MODE=cloud)
# QDRANT_API_KEY=your_qdrant_cloud_api_key
# QDRANT_URL=https://your-cluster.qdrant.io
